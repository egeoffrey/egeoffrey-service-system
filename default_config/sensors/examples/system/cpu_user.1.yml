calculate: avg_min_max
description: CPU User
format: float_1
retain: history
service:
  configuration:
    measure: cpu_user
  mode: pull
  name: system
  schedule:
    minutes: 10
    trigger: interval
unit: '%'
